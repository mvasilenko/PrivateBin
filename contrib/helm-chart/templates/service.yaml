apiVersion: v1
kind: Service
metadata:
  name: {{ template "privatebin.fullname" . }}
  labels:
    app: {{ template "privatebin.name" . }}
    chart: {{ template "privatebin.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "privatebin.name" . }}
    release: {{ .Release.Name }}
